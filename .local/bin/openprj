#!/bin/sh

prjdir="$HOME/prog/prj"
prj=`ls "$prjdir" | dmenu -p "Open project:" -l 5`
[ -z "$prj" ] && exit
# command -v code && code "$prjdir/$prj" && exit
cd "$prjdir/$prj" && "$TERMINAL" -e "$EDITOR"
